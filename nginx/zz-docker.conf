[www]
listen = 127.0.0.1:9000

pm = dynamic
pm.max_children = 100
pm.start_servers = 10
pm.min_spare_servers = 5
pm.max_spare_servers = 20
pm.max_requests = 500

; Prevenir erros de max_execution_time do apache
request_terminate_timeout = 300s
